# TypeScript 7 native compiler

This repo uses TypeScript 7 from the `typescript` npm package. We do not install `@typescript/typescript6` or `@typescript/native-preview`. After ESLint is gone, no tool needs the TypeScript 6 compiler API.
