# oxlint with oxc-config-ai-guardrails

This repo uses oxlint with the `base` and `typescript` presets from `oxc-config-ai-guardrails`. We deleted ESLint and its plugins. The React preset is not used because this repo has no JSX.
